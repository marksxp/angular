import { DirectivaPersonalizada01Directive } from './directiva-personalizada-01.directive';

describe('DirectivaPersonalizada01Directive', () => {
  it('should create an instance', () => {
    const directive = new DirectivaPersonalizada01Directive();
    expect(directive).toBeTruthy();
  });
});
