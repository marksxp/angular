import { DirectivaPersonalizada02Directive } from './directiva-personalizada-02.directive';

describe('DirectivaPersonalizada02Directive', () => {
  it('should create an instance', () => {
    const directive = new DirectivaPersonalizada02Directive();
    expect(directive).toBeTruthy();
  });
});
