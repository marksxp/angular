import { Transformar2Pipe } from './transformar2.pipe';

describe('Transformar2Pipe', () => {
  it('create an instance', () => {
    const pipe = new Transformar2Pipe();
    expect(pipe).toBeTruthy();
  });
});
