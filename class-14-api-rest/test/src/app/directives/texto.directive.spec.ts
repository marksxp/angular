import { TextoDirective } from './texto.directive';

describe('TextoDirective', () => {
  it('should create an instance', () => {
    const directive = new TextoDirective();
    expect(directive).toBeTruthy();
  });
});
